# 投影与预测

## 投影

**定理1**：设$S$为Hilbert空间$H$的一个闭子空

**定义2**：设$H=L^2(\Sigma,F,P)$为Hilbert空间$H$的一个闭子空

## 预期

**定义1**：设$H=L^2(\Sigma,F,P)$，$X,Y\in H$，$S$为$X$的所有可测函数生成的空间

为Hilbert空间

我们称为基于的最优预期。
(即，均方误差最小的预期)


上述最优线性预期方程不能确保方程的系数一定存在唯一
解，要确保存在一个唯一解，我们需要$\Gamma_n$可逆。

**定理5**：如果$\gamma_0>0$且$\lim\limits_{n\to\infty} \gamma_n =0$，那么$\forall n$，协方差矩$\Gamma_n$是非奇异的。

这个结论是关键的，但是证明并不关键。